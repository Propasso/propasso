

## Plan: Add Google Tag Manager to the website

Two snippets need to be added to `index.html`:

1. **GTM head script** — paste as high as possible in `<head>` (right after `<meta charset>`)
2. **GTM noscript fallback** — paste immediately after `<body>`

### File changed
- `index.html`

No other files need modification.

